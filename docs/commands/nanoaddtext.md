# Nano Add Text Command

nanoaddtext command is add text in file with GNU Nano.

# Examples 

```
input:
    <username>@<hostname>:~/home/<username> $# nanoaddtext file
output:
    <GNU Nano Screen>
    <waiting>
    <username>@<hostname>:~/home/<username> $#
```
