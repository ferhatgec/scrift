# FreeBrain Generator Command

genfrbr command is generates FreeBrain key code with input number.

# Examples

```
input:
    <username>@<hostname>:~/home/<username> $# genfrbr 10
output:
    ~++++++++++
    <username>@<hostname>:~/home/<username> $#
```
